#!/bin/bash
HASH=$(openssl passwd -6 live)
usermod -p "$HASH" root

cat <<'EOF' >> /etc/issue
***********************
* Root password: live *
***********************

If you need a TTY without autologin, use TTY 7-12.

EOF

