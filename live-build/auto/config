#!/bin/sh

lb config noauto \
  --mode debian \
  --distribution trixie \
  --debian-installer none \
  --binary-images iso-hybrid \
  --bootappend-live "boot=live quiet splash toram" \
  --linux-flavours amd64

